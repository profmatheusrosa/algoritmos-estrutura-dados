# Módulo 08: Exercícios - Recursão e Backtracking

## Exercício Prático 1: Soma Recursiva de Dígitos

Escreva uma função recursiva `soma_digitos(n)` que retorne a soma dos dígitos de um número inteiro positivo.
Ex: `soma_digitos(123)` -> `1 + 2 + 3 = 6`.

```python
def soma_digitos(n):
    # Dica: n % 10 pega o último dígito
    # n // 10 remove o último dígito
    pass
```

## Exercício Prático 2: Gerar Subconjuntos (Power Set)

Dado uma lista de números distintos, use Backtracking para gerar todos os subconjuntos possíveis.
Ex: `[1, 2]` -> `[], [1], [2], [1, 2]`

```python
def subconjuntos(nums):
    res = []
    # def backtrack(inicio, caminho_atual):
    # ...
    return res
```

---

[Voltar aos Links Rápidos](../README.md#links-rapidos)
